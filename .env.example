# ========================
# Supra Metrics Exporter
# ========================
# Set the role: rpc, validator, or both
ROLE=both

# ------------------------
# RPC Node Configuration
# ------------------------
# The RPC endpoint for your Supra node
RPC_URL=http://localhost:8545

# Path to the log file for the RPC node (optional for now)
RPC_LOG_PATH=/path/to/your/rpc.log

# ------------------------
# Validator Configuration
# ------------------------
# RPC URL for the validator (not required currently)
VALIDATOR_RPC_URL=http://localhost:9999

# Path to the validator log file
VALIDATOR_LOG_PATH=/path/to/your/validator.log

# Your validator's public key (used for log filtering)
NETWORK_PUBKEY=abcd1234abcd1234abcd1234

# ------------------------
# Exporter Settings
# ------------------------
# Port to expose the Prometheus /metrics endpoint
EXPORTER_PORT=9100

# How often to poll metrics (in seconds)
POLL_INTERVAL=10

# Canonical block explorer to compare stats with
PUBLIC_RPC_URL=https://rpc-testnet.supra.com/rpc/v1
